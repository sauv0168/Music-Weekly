<container>
    <row class="hero">
        <col>
            <div class="hero-intro">
                <h2>Meet our Musicians</h2><br>
                <ul id="musicians">{{#each musicians}}
                    <li class="musiciansLI">
                       <img src ="/uploads/{{this.image}}" width="30" height="30">
                        {{this.username}}
                    </li>
                    {{/each}}

                </ul>
            </div>
        </col>
    </row>
</container>